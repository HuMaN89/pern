const {Rating} = require('../models/models')
const ApiError = require('../error/ApiError')
const {Device, Rating, User} = require('../models/models')

class RatingCotroller {
    async setRating(req, res) {
        const {id } = User
    }
}